engine-strict=false

# Supply-chain-attack defense: refuse to install a package version published
# less than this many days ago. Needs npm >= 11.10.0 (see Dockerfile, which
# pins an npm upgrade in the frontend-builder stage for this).
min-release-age=14
