# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.pyright/

# Env / secrets / data
.env
data/
*.local

# Per-host compose tweaks. deploy/install.sh writes this one when the host has
# a microphone to pass through (deploy/mic-passthrough.override.yml is the
# template it copies); ignoring it is what keeps that choice out of git pull's
# way.
docker-compose.override.yml

# Local diarization model weights (mounted read-only into the diarization
# service by docker-compose.yml; fetched per services/diarization/README.md,
# not vendored in the repo).
models/

# Node / frontend
node_modules/
frontend/build/
frontend/.svelte-kit/

# OS
.DS_Store

# Tooling artifacts
.playwright-mcp/

# Local agent/editor tooling
.idea/
.claude/
CLAUDE.md
